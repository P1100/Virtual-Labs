import {TeamsCont
import {from} from 'rxjs';

.
Component;
}
from;
'./teams-cont.component';

describe('TeamsCont.Component', () => {
  it('should create an instance', () => {
    expect(new TeamsCont.Component()).toBeTruthy();
  });
});
